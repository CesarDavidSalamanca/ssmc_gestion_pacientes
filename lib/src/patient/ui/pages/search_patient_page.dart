import 'package:flutter/material.dart';

class SearchPatientPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hola Mundo Desde Consultar paciente'),
      ),
    );
  }
}
