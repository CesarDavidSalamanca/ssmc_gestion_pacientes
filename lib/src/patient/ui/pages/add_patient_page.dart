import 'package:flutter/material.dart';

class AddPatientPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hola Mundo Desde Registrar paciente'),
      ),
    );
  }
}
