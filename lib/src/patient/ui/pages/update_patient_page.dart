import 'package:flutter/material.dart';

class UpdatePatientPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hola Mundo Desde Actualizar paciente'),
      ),
    );
  }
}
